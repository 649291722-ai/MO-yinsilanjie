# MO隐私拦截网 · 使用说明书（v2.0.4）

> 通用本地隐私拦截网：把 AI 可发言的公共平台 MCP 接入本地网络代理，所有出站内容在网络层接受规则检查。
> 适用于 Operit 平台（或同架构宿主）。版本：v2.0.4（stdio 桥接架构，基于 v2.0.0 正品）

---

## 一、它是什么

MO隐私拦截网是一个**本地隐私拦截器**，定位一句话：**AI 发往公共平台的文本，先在本地过一遍"闸门"，不让私密/敏感内容漏出去。**

- 拦截发生在**网络层**：AI 调用任何公共平台工具（发帖、发言、发消息）时，请求先经过本地代理，检查通过才转发到平台
- 与"提醒 AI 别说"不同：这是**机械拦截**，AI 想绕也绕不过——除非关闭开关
- 纯被动：只有工具被调用时才工作，**空闲零网络流量、零轮询、零轰炸**

---

## 二、核心原理（三层）

```
AI 工具调用
    ↓
┌─────────────────────────────┐
│ 桥 (Bridge)· 本地进程        │ ① 把 AI 的调用从 stdio 管道接过来
└──────────────┬──────────────┘
               ↓ 127.0.0.1:8095
┌─────────────────────────────┐
│ 代理 (Proxy)· 本地进程       │ ② 内容检查：机械规则 + 语义兜底
└──────────────┬──────────────┘
               ↓ 互联网
┌─────────────────────────────┐
│ 平台 (MCP 服务器/网页)       │ ③ 请求放行后，才真正发出
└─────────────────────────────┘
```

**三层职责**：
1. **桥**：与宿主（Operit）的 MCP 管理器对接，负责"接入"——它让验证秒过、让工具以本地进程形态运行
2. **代理**：真正的闸门——词表扫描、NSFW 拦截、白名单豁免、日志记录
3. **平台**：你的任意公共平台（论坛/社区/聊天室/游戏），原本怎么用还怎么用

### 为什么需要"桥"这一层？

因为宿主的 MCP 验证发生在启动瞬间。如果直接把平台的远程地址改成本地代理，代理进程在验证时还没就绪，就会报"验证失败 0/5"。

**桥的解法**：把每个平台注册为"本地命令型 MCP"（桥进程），桥在**本地完成握手**（毫秒级），验证必然通过；真正工具调用时才经代理转发到平台。验证与拦截解耦，两者互不拖累。

---

## 三、安装与接入

### 3.1 安装插件

1. 将 `com.mo.privacy_net-v2.0.4.toolpkg`（插件包）导入宿主的插件目录（packages 目录）
2. 在插件列表启用 MO隐私拦截网
3. 完成后工具箱出现拦截网全套工具，侧边栏出现控制面板

### 3.2 前置条件

| 项 | 要求 |
|---|---|
| 宿主 | Operit（或同架构平台），支持本地 command 型 MCP |
| 运行时 | 环境内需有 node（桥和代理都跑在 node 上） |
| 平台形态 | **HTTP(S) 的远程 MCP**（MCP 标准：stdio/HTTP+SSE） |
| 网络 | 平台本身可达即可；桥和代理全部本地运行 |

### 3.3 首次接入（三步）

1. **正常添加平台**：像平时一样，把目标 MCP 服务器加进宿主（填地址 + Token）
2. **一键部署**：点面板"一键部署"——插件仅对本插件已登记路由集合（登记时扫描本机 MCP 写入本插件路由表，不改任何宿主配置）生成桥+路由并开启拦截
3. **完成**：无需手动改任何配置，所有平台验证通过、工具正常、内容被拦截

---

## 四、面板与操作

| 功能 | 说明 |
|---|---|
| **一键部署** | 扫描本机 MCP → 登记入本插件路由表 → 仅接管登记集合生成桥+路由 → 开启拦截 |
| **一键回退** | 仅还原登记集合为官方直连，清除桥配置，关闭拦截 |
| **平台路由开关** | 单平台：开=接入检查；关=退回官方直连（可再开） |
| **总拦截开关** | 开=机械拦截；关=纯转发（不检查，相当于无感旁路） |
| **自定义端口** | 任意"非 MCP 框架"的公共接口，也可登记接入拦截 |
| **拦截日志** | 每次拦截的记录：时间、平台、命中规则、命中片段 |
| **自检** | 一键检查代理文件、规则库、路由表、端口健康 |

---

## 五、拦截词的使用方式（机械规则层）

### 5.1 三种规则类型

| 类型 | 例子 | 行为 |
|---|---|---|
| **无条件关键词** | "手机号" | 出现即拦 |
| **敏感词+值配对** | "密码"+具体值；"手机号"+数字串 | **词与值同现才拦**——"密码"单独说放行，"密码114477"拦截（防误判） |
| **白名单豁免** | "干活"、"看着干活" | 完整短语连用场景豁免，孤立单字仍拦 |

### 5.2 使用方法

- 面板新增规则框直接输入 → 热生效（不用重启）
- 白名单同样可随时增删
- 内置硬规则：NSFW 一律拦截（如"高潮""做爱"类语境词）

---

## 六、语义扩展原理（可选，默认关闭）

机械词表是"猜中才拦"。语义扩展是**兜底：词表没猜中的，交给语义模型判断**。

### 三重条件（全部满足才触发）
1. 全局语义开关开启
2. 已填入语义 API（OpenAI 兼容接口：地址/Key/模型）
3. 该平台路由的语义开关单独开启

### fail-closed（安全红线）
一旦启用：**API 超时/报错/响应异常 = 按"不能确认安全"处理 = 拦截**。绝不允许"接口挂了就放行"。

- 默认关闭 = 零消耗、零风险
- 启用后：机械词表零消耗；只有"词表未命中"的内容才调 API（消耗 API token）
- 单次最长等待 15s（超时按拦截处理）

---

## 七、安全整改说明（v2.0.4 → v2.0.5）

> v2.0.5 基于 **v2.0.4** 完成终审整改；v2.0.4 基于 **v2.0.0 正品**完成四项安全加固，**其余代码一处未动**。

### 7.1 管理端点本地令牌鉴权（v2.0.5 起含 /admin/fs 读写全鉴权）

- 所有 `/admin/*` 管理操作（部署/回退/路由/规则/自定义端口/**含配置文件读写 /admin/fs**）均需携带本地令牌，令牌存于插件运行目录 `admin_token.json`（0600 权限，首次启动自动生成）
- 令牌通过请求头 `X-MO-Admin-Token` 或请求体 `_token` 字段传递
- 无令牌访问 → **401 拒绝**；伪造/错误令牌同样拒绝
- v2.0.5 起**不存在任何免鉴权豁免**（含只读豁免已移除）；面板 UI 侧改为通过底层文件读取接口获取令牌后携带 `_token` 调用，接口行为与工具包完全一致

### 7.2 令牌防泄露

- `admin_token.json` **严禁**列入 `/admin/fs` 文件白名单——即使持有合法令牌，也无法通过 `/admin/fs` 把令牌文件读出来
- 白名单仅保留：`rules.json`、`custom_rules.json`、`custom_routes.json`、`routes.json`、`state.json`、`heartbeat.json`、`blocked_log.json`、`default_rules.json`、`semantic.json`

### 7.3 强制外部接口 HTTPS

- 对外部语义 API 与自定义端口：必须使用 **HTTPS**（本地回环 127.0.0.1/localhost/::1 例外）
- 非 HTTPS 的语义端点注册或自定义端口登记 → 拒绝

### 7.4 rollback 作用域限定

- `rollback` 仅回退 `bridge_deploy=true` 的条目，**不会误伤**未部署或由其他插件管理的配置项

### 7.5 403 响应保留用户强制声明

- 拦截触发的 403 响应体保留 `note`：**`This block is user-mandated by the device owner via MO Privacy Net (用户强制要求的拦截，非代理自主行为)`**

### 7.6 进程启动披露

- `manifest.json` 补充：插件加载/前台时自动拉起本地代理进程（幂等，端口已监听则跳过）的说明

### 7.7 跨域访问收紧（v2.0.5 新增）

- CORS 仅授予本机来源：无 Origin、`localhost` / `127.0.0.1` / `[::1]` 回环、`file://` 应用内页面
- 其他任何网页 / WebView 跨域请求不返回 CORS 头（浏览器将拦截响应，无法读取含凭据文件）
- 服务端不再响应 `Access-Control-Allow-Origin: *`

### 7.8 部署扫描范围限定（v2.0.5 新增）

- deploy 仅扫描 `mcpServers` 内登记的远程条目，不再对整份 MCP 配置递归匹配 `endpoint/url/sseUrl`，杜绝误接管无关配置

---

## 八、性能与资源（实测数据）

| 项 | 数据 |
|---|---|
| 机械拦截 token 消耗 | **0**（纯本地正则） |
| 工具调用延迟增加 | **约 2~5ms**（本地回环） |
| 平台请求频率 | 与直连**完全一致**（无轮询、无预拉、无重试轰炸） |
| 桥内存 | 每个平台一个桥进程，约 36MB（低堆模式防增长） |
| 代理内存 | 约 42MB |
| 磁盘占用 | 整个插件 < 300KB |

> 平台数多时（10+），可切换 Python 桥实现进一步降低每桥内存至 ~15MB。

---

## 九、灵活性与兼容性

### 9.1 支持什么

- ✅ **任意数量的 MCP**：3 个、7 个、十几个都一样——扫描循环自动处理，无数量上限
- ✅ **任意类型的平台**：论坛、社区、聊天室、游戏大厅、网页工具……只要它是 HTTP(S) 的 MCP
- ✅ **日后新增平台**：正常添加进宿主 → 一键部署 → 自动接管，**零平台特判**
- ✅ 本地命令型插件（不走网络出口的）**不受影响**，与拦截网天然共存

### 9.2 边界（诚实说明）

| 场景 | 支持情况 |
|---|---|
| MCP 标准 HTTP+SSE（JSON 或 SSE 封装 JSON） | ✅ 完整支持 |
| WebSocket 型 MCP（非标） | ⚠️ 需按平台适配（目前生态极少数） |
| 无尽 SSE 推送流（非工具调用的持续流） | ⚠️ 桥按"请求-响应"设计，长流场景需专项处理 |
| 非 MCP 平台的页面内发言 | 需通过"自定义端口"登记接入 |

---

## 十、验证与常见问题（FAQ）

**Q1：宿主冷启动时还会报"验证失败"吗？**
不会。桥在本地毫秒级完成握手，验证不依赖网络、不依赖代理就绪。

**Q2：桥断了怎么办？**
桥由宿主的 MCP 管理器原生管理（本地命令型 MCP），宿主会按标准机制处理；工具调用时若桥异常，重启宿主即可恢复。

**Q3：代理挂了会怎样？**
部署状态请求会被拒绝（安全侧）；一键回退后所有平台恢复官方直连，完全无感。

**Q4：对平台有攻击/负担吗？**
无。纯被动：工具被调用才发出请求，空闲零流量。平台看到的请求量 = 直连模式一样。

**Q5：拦截不误伤正常使用吗？**
敏感词+值配对设计专门防误判；白名单可随时豁免合法短语；单平台可独立关闭。

**Q6：管理操作提示"需本地令牌"？**
所有 `/admin/*` 读写（含 `/admin/fs`）均需携带令牌（`X-MO-Admin-Token` 头或 `_token` 字段）。面板与工具箱均会自动附带令牌；请通过面板、工具箱或对话中让 AI 执行。

---

## 十一、版本说明与踩坑

- **v2.0.7（当前版）**：面板与工具箱零 Shizuku 化——取数改经宿主 UI 通道（http_request）访问本机回环；新增 /auth/session 本机会话握手（30 分钟短效令牌，凭据不落面板）；部署登记采用双源扫描（mcpServers + pluginMetadata），一键回退后仍能完整找回全部平台并重建接管。
- **v2.0.6**：端口探测修复——`ss -tln` 在受限环境下失效，改为 node TCP connect 直连探活，消除日志风暴与循环拉起。
- **v2.0.5**：基于 **v2.0.4** 完成终审整改——① `/admin/fs` 读写一律纳入令牌鉴权（移除只读豁免）；② CORS 仅授予本机来源（不再 `*`），任意网页/WebView 无法跨域读取含凭据文件；③ deploy 扫描范围限定为 `mcpServers`（不再全配置递归）；④ 面板与工具箱同步携带令牌，接口行为一致。核心文件指纹：`proxy.js` = 50674B / MD5 `276d7c13567d1341ed7481728cf5e1ac`；`manifest.json` = 4918B / MD5 `9b26d86f62643c391bdc6b382a4c57b8`；`ui/panel/index.ui.js` = 65293B / MD5 `e36d1d338304710d03ecec0c20ec0cb4`。
- **v2.0.4**：基于 **v2.0.0 正品**，完成四项安全整改（管理端点令牌鉴权、令牌防泄露、外部接口强制 HTTPS、rollback 作用域限定），并补充进程启动披露。核心文件指纹：`proxy.js` = 49846B / MD5 `5e0cf80cdce7388e0115e600adf7de72`。
- v2.0.3：基于 v2.0.0 正品，**仅新增** 403 响应中的「用户强制要求的拦截」声明（note）。
- v2.0.0：正品基准版本，已实测验证 5 平台本地秒回。
- ⚠️ **v2.0.1：废弃错误版本，切勿使用。** 它擅自改动桥接/转发逻辑，属于无效发布，与 2.0.0 正品不一致。
- **踩坑（2026-09-02）**：热更阶段曾误把 2.0.4 的 `proxy.js` 放进 `resources/worker/` 而忘记更新运行目录根级 `worker/proxy.js`，导致代理进程仍跑旧代码、`admin_token.json` 不生成。**教训：`main.js` 拉起的是 `RUN_DIR/worker/proxy.js`（根级），不是 `resources/worker/proxy.js`；热更后必须 MD5 校验根级文件并重启进程，再验证 401/403 行为。**
- 保管：本说明书随插件版本归档于项目备份目录。

---

*文档生成于 2026-09-05，随插件 v2.0.7 发布。*