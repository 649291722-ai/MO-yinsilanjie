# 市场提交文案 · MO隐私拦截网 v2.0.7

## 一句话简介（列表页描述）

把 AI 可发言的公共平台接入本地代理，出站内容在网络层机械过滤——敏感词、NSFW、凭据、IP、路径，命中即拦截，内容物理上不出设备。纯被动、零负担、无平台特判，任何 MCP 平台都能接。

## 详情页正文

## 它解决什么问题

AI 靠「自律」检查发言内容，存在漏网可能。本插件把检查点下沉到网络层：请求在离开设备前必须经过本地代理（127.0.0.1:8095），规则引擎逐字检查请求体，命中敏感规则直接返回 403——内容物理上无法离开设备。

## 核心特性

- **局部可调 + 全局兜底**：单条路由独立开关；一键部署只接管登记集合；一键回退只还原登记集合，绝不触碰无关配置
- **自定义端口桥**：任意平台 endpoint 可接本地桥入口，独立卡片开关
- **语义扩展审查（可选）**：接入 OpenAI 兼容接口做深度审查，超时/出错一律拦截（fail-closed），默认关闭零消耗
- **公共发言闸门**：被拦提示「检测出私密信息，已拦截内容返回」，同一平台三次拦截直接拒发
- **拦截日志**：命中片段可回溯
- **敏感词+具体值配对**：如「密码114477」拦而「密码」不拦，防误判
- **关键词热编辑**：用户自由增删拦截词，立即生效
- **零 Shizuku 依赖**：面板与工具箱取数走宿主 UI 通道（http_request）访问本机回环，任何设备环境可运行

## 安全设计

- 所有管理端点（含配置文件读写）一律 **本地令牌鉴权**（0600，令牌不可经文件接口读取）
- 新增本机回环会话握手（/auth/session，仅 127.0.0.1 可达）：30 分钟短效会话令牌，凭据不落面板、不进出网络
- CORS 仅授予本机来源，任意网页/WebView 无法跨域读取含凭据文件
- 外部语义接口与自定义端口目标强制 HTTPS（本地回环例外）
- 登记采用**双源扫描**（mcpServers + pluginMetadata）仅写入本插件路由表、不改任何宿主配置；deploy/rollback/route 仅作用于本插件已登记集合（routes.json 中 bridge_deploy 标记），一键回退后仍可一键完整找回全部已安装平台重建登记，无需手动登记

## 进程披露

本插件在注册及宿主应用创建/前台时，会自动通过宿主终端拉起一个常驻本地代理进程（node worker/proxy.js，仅监听 127.0.0.1:8095）；被接管的公共平台出站数据流经该本地进程检查后转发。

## 零负担保证

纯被动代理：无轮询、不扫描平台、不重试轰炸、不缓存不重放；只检查路过本机的请求体；未命中的请求原样转发。平台看到的请求量与直连模式一致。

## 安装（引用 GitHub Release 资产）

1. 下载安装包：com.mo.privacy_net-v2.0.7.toolpkg
   https://github.com/649291722-ai/MO-yinsilanjie/releases/download/v2.0.7/com.mo.privacy_net-v2.0.7.toolpkg
2. 在 Operit 中安装该工具包
3. 侧边栏打开「MO隐私拦截网」面板 -> 一键部署 -> 选择要接管的平台

## 版本说明

- **v2.0.7**：面板/工具箱零 Shizuku 化（宿主 UI 通道 + 回环会话握手）；部署登记双源扫描
- **v2.0.6**：端口探测修复（node TCP connect 直连探活替代 ss -tln，消除日志风暴与循环拉起）
- **v2.0.5**：管理端点全量令牌鉴权、CORS 收紧、外部接口强制 HTTPS、rollback 作用域限定、进程披露
- **v2.0.4**：基于 v2.0.0 正品的四项安全整改（管理端点令牌鉴权、令牌防泄露、HTTPS 强制、rollback 收窄）
- ⚠️ v2.0.1 为废弃错误版本，切勿使用

源码仓库：https://github.com/649291722-ai/MO-yinsilanjie
